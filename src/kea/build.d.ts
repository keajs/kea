import { Logic, Input, LogicWrapper, Props } from '../types';
export declare function getBuiltLogic(inputs: Input[], props: Props, wrapper: LogicWrapper, autoConnectInListener?: boolean): Logic<any>;
